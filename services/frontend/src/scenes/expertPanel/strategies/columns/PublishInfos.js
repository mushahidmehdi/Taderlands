import React from "react";
import { Grid, Typography, Box } from "@mui/material";

export default function PublishInfos() {
	return (
		<Grid container justifyContent={"center"}>
			<Grid item xs={8}>
				<Typography color={"#0F20E8"}>Yayınlama Bilgileri</Typography>
			</Grid>
		</Grid>
	);
}
